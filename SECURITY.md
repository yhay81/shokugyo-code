# Security

Report vulnerabilities privately through GitHub security advisories. The service has no authentication or private-data workflow. It accepts only small same-origin, allowlisted telemetry payloads, applies strict security headers, treats official data as text, and expires event rows after 35 days.
