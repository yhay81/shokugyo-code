# Stack

- Cloudflare Workers and custom domain
- Hono JSX server-rendered public pages
- Vite+ check, test, build, and development
- Verified static JSON and browser-only search/comparison
- D1 allowlisted anonymous telemetry

Better Auth is intentionally absent because the only saved state is four public classifications on one device.
